# Python dependencies for lessonCreation.py
# Install with: pip install -r requirements.txt

# Pin cryptography to a version with pre-built wheels (avoids Rust/maturin build)
cryptography>=41.0.0,<43.0.0

# Google GenAI SDK for Vertex AI
google-genai>=1.60.0

# Standard library modules used (no install needed):
# - os, re, json, time, uuid, dataclasses, typing
